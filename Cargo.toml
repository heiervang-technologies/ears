[package]
name = "ears"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
nix = { version = "0.29", features = ["signal", "process"] }
thiserror = "2.0"

[dev-dependencies]
tempfile = "3.16"
