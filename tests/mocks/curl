#!/usr/bin/env bash
# Mock curl for testing
if [[ "$*" == *"/health"* ]]; then
    echo "OK"
    exit 0
elif [[ "$*" == *"/inference"* ]]; then
    echo '{"text":"Mock transcription"}'
    exit 0
else
    exit 0
fi
