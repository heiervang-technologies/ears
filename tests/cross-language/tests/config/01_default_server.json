{
  "name": "Default whisper server is localhost:8178",
  "description": "Verify that the default server URL is correctly configured",
  "setup": {
    "env": {
      "HOME": "$TMPDIR/home",
      "XDG_RUNTIME_DIR": "$TMPDIR/runtime"
    }
  },
  "command": {
    "args": ["--server"]
  },
  "assertions": {
    "exit_code": 0,
    "stdout_contains": ["http://127.0.0.1:8178"]
  }
}
